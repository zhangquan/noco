{
  "name": "noco",
  "version": "1.0.0",
  "private": true,
  "description": "Noco Monorepo - AI Agent-friendly database and tools",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
